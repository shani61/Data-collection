document.getElementById("updateForm").addEventListener("submit", function(event) {
    event.preventDefault();
    const updateName = document.getElementById("updateName").value;
    const newTelephone = document.getElementById("newTelephone").value;
    // Here you'll make an AJAX or Fetch request to your server
    // to update the data in the Excel sheet.
});
